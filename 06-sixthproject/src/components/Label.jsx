import { StyleSheet, Text, View } from 'react-native';

import React from 'react';

const Label = ({ label, children, style, subLabel, SubLavelStyle }) => {
  return (
    <View>
      <Text>Label</Text>
    </View>
  );
};

export default Label;

const styles = StyleSheet.create({});
